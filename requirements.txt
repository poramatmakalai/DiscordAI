# ============================
# Discord
# ============================

discord.py>=2.4.0

# ============================
# Google Gemini
# ============================

google-genai>=2.10.0

# ============================
# Environment
# ============================

python-dotenv>=1.0.1

# ============================
# HTTP (ใช้โดย discord.py และ download_attachments ใน main.py)
# ============================

aiohttp>=3.11.0

# ============================
# DOCX (ai/gemini.py::_read_docx)
# ============================

python-docx>=1.1.2

# ============================
# หมายเหตุ: dependency ที่ตัดออกเพราะไม่มีจุดไหนในโค้ด import ใช้งานจริง
# (เช็คด้วย grep ทั้งโปรเจกต์แล้ว) — ถ้าจะเพิ่มฟีเจอร์ที่ต้องใช้ค่อยเติม
# กลับมาตอนนั้น:
#   tenacity        (retry เขียนเองด้วย decorator ใน ai/gemini.py แล้ว)
#   httpx           (โปรเจกต์ใช้ aiohttp ผ่าน discord.py/download_attachments)
#   Pillow          (ยังไม่มีโค้ดประมวลผลรูปภาพ ส่งเป็น bytes ตรงให้ Gemini)
#   PyMuPDF         (PDF ส่งเป็น bytes ตรงให้ Gemini ไม่ได้ parse เอง)
#   pandas / openpyxl (ยังไม่รองรับไฟล์ .xlsx/.xls)
#   pytesseract     (ยังไม่มี OCR)
#   markdown        (ยังไม่มีการ render markdown เป็น HTML)
#   beautifulsoup4 / lxml (ยังไม่มีการ parse HTML)
#   validators      (ยังไม่มีการ validate URL)
#   orjson          (ai/gemini.py ใช้ stdlib json อยู่แล้ว)
# ============================