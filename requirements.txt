# ============================
# Discord
# ============================

discord.py>=2.4.0

# ============================
# HTTP (ใช้เรียก Ollama local + scrape DuckDuckGo + โหลดไฟล์แนบจาก Discord)
# ============================

aiohttp>=3.11.0

# ============================
# Environment
# ============================

python-dotenv>=1.0.1

# ============================
# Document Reading (ai/document_reader.py — อ่านไฟล์แบบ local ทั้งหมด)
# ============================

python-docx>=1.1.2
pypdf>=5.1.0

# ============================
# Web Search (ai/web_search.py — parse ผล DuckDuckGo, ไม่ใช้ API key)
# ============================

beautifulsoup4>=4.12.3

# ============================
# หมายเหตุ: ไม่มี google-genai อีกต่อไป — โปรเจกต์นี้ไม่เรียก API ของ
# ที่ไหนที่ต้องใช้ key เลย ยกเว้นการ scrape DuckDuckGo (ไม่ต้องใช้ key
# แต่ยังต่อเน็ตออกไปอยู่ ปิดได้ที่ config.ENABLE_WEB_SEARCH = False)
#
# ต้องติดตั้ง Ollama แยกต่างหาก (ไม่ใช่ pip package): https://ollama.com/download
# ============================
